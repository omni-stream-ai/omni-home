---
import TopicPage from "../components/TopicPage.astro";
import { topicPages } from "../content/seo";

const page = topicPages.find((entry) => entry.slug === "voice");
if (!page) {
  throw new Error("Voice page content is missing.");
}
---

<TopicPage page={page} />
