---
import TopicPage from "../components/TopicPage.astro";
import { topicPages } from "../content/seo";

const page = topicPages.find((entry) => entry.slug === "bridge");
if (!page) {
  throw new Error("Bridge page content is missing.");
}
---

<TopicPage page={page} />
