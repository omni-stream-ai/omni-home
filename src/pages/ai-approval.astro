---
import TopicPage from "../components/TopicPage.astro";
import { topicPages } from "../content/seo";

const page = topicPages.find((entry) => entry.slug === "ai-approval");
if (!page) {
  throw new Error("AI approval page content is missing.");
}
---

<TopicPage page={page} />
