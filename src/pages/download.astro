---
import TopicPage from "../components/TopicPage.astro";
import { topicPages } from "../content/seo";

const page = topicPages.find((entry) => entry.slug === "download");
if (!page) {
  throw new Error("Download page content is missing.");
}
---

<TopicPage page={page} />
